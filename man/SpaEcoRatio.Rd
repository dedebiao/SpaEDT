% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpaEcoRatio.R
\name{SpaEcoRatio}
\alias{SpaEcoRatio}
\title{SpaEcoRatio
Core Logic:
\enumerate{
\item KNN finds neighbors (excluding itself);
\item Divide the type count by k to get the proportion;
\item Write back to the main table without destroying the original columns.
}}
\usage{
SpaEcoRatio(cell_data, x = "x", y = "y", type = "type", k = 50)
}
\arguments{
\item{cell_data}{data.frame; It must contain x, y (unit: µm) and any other column}

\item{x, }{y coordinate column names (default "x", "y")}

\item{type}{Cell type column name (default "type")}

\item{k}{Number of nearest neighbors (default 50, excluding itself)}
}
\value{
The original table adds a new column "{type}_proportion" (0-1 consecutive)
}
\description{
SpaEcoRatio
Core Logic:
\enumerate{
\item KNN finds neighbors (excluding itself);
\item Divide the type count by k to get the proportion;
\item Write back to the main table without destroying the original columns.
}
}
\examples{
cell_data <- SpaEcoRatio(cell_data,x = "x",y = "y",type = "type",k=50)
}
